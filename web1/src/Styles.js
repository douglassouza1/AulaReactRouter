import styled from 'styled-components';

export const Titulo = styled.h1`
    color:blue;
    font-size:35px;`

;
export const Textopq = styled.h4`

    color: red;
    font-size: ${props => `${props.fontSize}px`};

`;
